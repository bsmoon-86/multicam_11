# [실습 3] 데이터 분석 (Analysis)
# 미션:
# 1. 'Date' 컬럼을 날짜 형식으로 변환해줘.
# 2. 'Total_Sales' 컬럼을 만들어줘 (Price * Quantity).
# 3. 'City'별로 총 매출(Total_Sales)이 높은 순서대로 정렬해서 보여줘.

import pandas as pd
df = pd.read_csv('sales_data.csv')
# (전처리 로직이 포함되어야 정확합니다. 앞선 실습 코드를 포함해서 질문하세요)

# AI가 생성한 분석 코드를 아래에 붙여넣으세요.
